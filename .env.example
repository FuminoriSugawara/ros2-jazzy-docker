# Copy this file to `.env` and adjust values for your deployment. Source it
# before running `zenoh_cli_sample.py` so the script knows where to connect.
#
# Endpoints that `zenoh_cli_sample.py`, the zenoh bridge, and other tools
# should use. Comma-separate multiple endpoints if needed.
ZENOH_CONNECT=tcp/your-zenohd-ip:7447
